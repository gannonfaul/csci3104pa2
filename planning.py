def free_time_intervals(interval_lst, T):
    # First design the algorithm on pen/paper before you attempt this
    # Take interval at interval_lst[0]
    # Create two intervals o either side of interval_lst
    	# i.e. 0 - l, and u - T
	# Check if intervals overlap with any other exisitng ones
	#IDEA 2:
		# Create hash table  with slots 0, 1, ..., T
		# Fill table completly
		# empty slots corresponding to input intervals
		# create intervals from remaining hash table slots when interval_lst reaches end 
    return None

def max_logged_in(interval_lst,T):
    # First design the algorithm on pen/paper and solve a few examples.
    return None